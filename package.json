{
    "name": "data-space-example",
    "description": "Dataspace service chain",
    "version": "0.0.1",
    "type": "module",
    "scripts": {
        "typecheck": "tsc",
        "typecheck:watch": "tsc --watch",
        "lint": "eslint",
        "check": "npm run typecheck && npm run lint",
        "compile": "node esbuild.mjs",
        "compile:production": "node esbuild.mjs --minify",
        "assemble": "npm run compile",
        "assemble:production": "npm run compile:production",
        "test": "vitest run --maxWorkers 1 --minWorkers 1",
        "test:unit": "vitest run --project unit",
        "test:integration": "vitest run --project integration",
        "test:e2e": "vitest run --project e2e",
        "coverage": "vitest run --coverage",
        "build": "npm run check && npm run compile && npm run test:unit",
        "build:production": "npm run check && npm run compile:production",
        "servers": "npm run assemble && concurrently -k \"node dist/server-runner/analytics-company-runner.js\" \"node dist/server-runner/moodle-runner.js\" \"node dist/server-runner/q-s-world-university-runner.js\" \"node dist/server-runner/university-runner.js\"",
        "servers:production": "npm run assemble:production && concurrently -k \"node dist/server-runner/analytics-company-runner.js\" \"node dist/server-runner/moodle-runner.js\" \"node dist/server-runner/q-s-world-university-runner.js\" \"node dist/server-runner/university-runner.js\"",
        "servicechain": "node dist/service-chain/collect-university-ranking-data-runner.js"
    },
    "dependencies": {
        "express": "^5.2.1",
        "nanoid": "^5.1.11"
    },
    "devDependencies": {
        "@eslint/js": "~10.0.1",
        "@types/express": "^5.0.0",
        "@types/node": "~25.3.5",
        "@vitest/coverage-v8": "^3.2.4",
        "concurrently": "~9.2.1",
        "esbuild": "^0.28.0",
        "eslint": "~10.0.3",
        "globals": "~17.4.0",
        "typescript": "~5.9.3",
        "typescript-eslint": "^8.56.1",
        "vitest": "^3.2.4"
    },
    "engines": {
        "node": ">=20.0.0"
    }
}
